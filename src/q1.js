let VerificarStr = (str) => { // RECEBE A STRING
    if (typeof str != "string") { // VERIFICA SE É UMA STRING E COMPARA COM O TIPO "STR"
        console.log('false') // RETORNA FALSE SE NÃO FOR STRING
    } else {
        console.log('true')// RETORNA TRUE SE FOR STRING
    }
}