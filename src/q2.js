let verificarVazia = (str) => {// RECEBE A STRING
    if (str.length === 0){ // VERIFICA SE O TAMANHO DA STRING É IGUAL A 0
        console.log('true')   // RETORNA TRUE SE FOR VAZIA
    } else {
        console.log('false') // RETORNA FALSE SE NÃO FOR VAZIA
    }
}